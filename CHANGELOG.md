# 维护更新记录

## 订阅制上线（Pro 会员）- 2026-06-22

**接入 StoreKit 2 内购，给已埋好的 Pro 门控通电；本期开放「去水印」与「拍照识别每日限额」两项现成权益**

- **支付层（StoreKit 2）**：新增 `WODTrack/Services/SubscriptionManager.swift`，作为订阅态唯一真相源——拉取年/月两档自动续期商品、按 `Transaction.currentEntitlements` 计算权益、`Transaction.updates` 全程监听续期/退款、支持购买与「恢复购买」。权益绑 Apple ID 由系统自动跨设备同步，**不经 CloudKit**，不触碰本地 store。建在 App 层（`WODTrackApp`）持有，避免随 iCloud 容器重建而丢失监听；算出结果回写 `AppState.applyEntitlement(isPro:)`，全站既有 `appState.isPro` 读取点零改动生效。
- **付费墙页面**：新增 `WODTrack/Views/Subscription/PaywallView.swift`（原生 SwiftUI），展示权益列表、年/月价格卡（年付标「最划算」，价格取自 StoreKit 本地化 `displayPrice`）、订阅/恢复购买、用户协议与隐私政策链接。按触发场景（去水印 / Pro 模板 / OCR 限额 / 主动升级）切换标题文案。
- **权益①去水印**：卡片生成页选 Pro 模板触发的占位 alert 改为弹出 Paywall；水印显隐逻辑早已随 `isPro` 工作，订阅成功后预览自动去除「@迹录 WOD」水印。
- **权益②拍照识别限额**：新增 `WODTrack/Services/OCRUsageTracker.swift`，免费用户每**自然日**最多识别 2 次（跨天自动归零），第 3 次起在记录流程弹 Paywall；Pro 不限次数。仅用户主动发起时计数，超时/网络重试不重复计。
- **订阅入口**：「我的」页顶部新增订阅卡片——未订阅显示「升级 Pro」进 Paywall，已订阅显示「Pro 会员」并跳系统「管理订阅」。
- **本地测试**：新增 `WODTrack/StoreKit/WODTrack.storekit` 配置并关联 Scheme，可在模拟器离线自测购买/续期/退款；正式商品需在 App Store Connect 按相同商品 ID 创建。
- 注：Pro 高级卡片模板与高级数据统计的门控已就绪（`CardStyleConfig.proStyles` 暂留空），内容设计完成后另行上线。

## 英文版上线 - 2026-06-22

**全量本地化：同一 App 跟随系统语言自动中/英文切换（品牌英文名 WOD Trace）**

- **运行时语言层**：新增 `WODTrack/Config/Localization.swift`，提供 `AppLanguage.isEnglish` 与各数据结构的 `localized*` 取值（动作名/简介/步骤/要点/降阶、分类、进阶路线名/说明、难度 `SkillTier`），并为分类/子类（动作模式）、计量、难度等固定名词内置中英查表。技能树、动作详情、进阶路线、二级分类页等展示处统一改用 `localized*`，英文缺失时自动回退中文。
- **动作库全英文**：`SkillDefinition` 新增 `englishIntro/englishSteps/englishKeyPoints/englishScaling` 字段，`gen_skilldata.py` 改造读取 xlsx 英文列生成。123 个动作（体操 56 / 举重 67）的名称、简介、步骤、要点、降阶替代英文全部到位。
- **进阶路线全英文**：`ProgressionPath` 新增 `englishName/englishIntro`，`gen_progressionpath.py` 读取「路径说明_En」生成，路线英文名（Jump Rope / Handstand / Bar Muscle-Up / Pull-Up / Toes-to-Bar）随脚本内查表维护。5 条路线名称与说明英文齐全。
- **UI 文案 String Catalog**：以 `Localizable.xcstrings` 取代失效的 `Localizable.strings`，全量 UI 文案（按钮/标题/提示/弹窗/错误/状态等约 280 条）补齐英文；自定义组件 `title/label` 等参数改为 `LocalizedStringKey`，枚举展示标签与动态串改用 `String(localized:)`，确保运行时真正切换。
- **App 名称与系统权限**：新增 `InfoPlist.xcstrings`，英文环境 App 名显示 `WOD Trace`，相机/相册权限说明同步本地化。
- **细节打磨**：英文首页标语「Track every workout.」（单行不截断）；Add Entry 日期改 `yyyy.MM.dd`（不再出现中文「年月日」）；首页统计「Check-ins」单行对齐；技能树仪表盘状态、动作详情等残余中文清零。
- **数据安全**：本地化仅作用于展示层，不改任何 `@Model` 字段；已持久化/同步的成绩文本（如「X 轮」）保持原样，避免影响 CloudKit 与多设备一致性。
- 工程已声明 `knownRegions = zh-Hans, en`，跟随系统语言；中文为开发语言，行为不变。

## 1.9.7版本 - 2026-06-19

**动作库示范视频上线（腾讯云 COS + CDN）**

- **真实视频替换占位符**：动作详情页顶部的「视频 · 即将上线」占位方块，对已上传视频的动作改为播放真实示范视频——静音、自动无缝循环（`AVQueuePlayer` + `AVPlayerLooper`），类似动图。未上传视频的动作仍显示原占位符。
- **按 id 拼地址，无需后端**：视频托管在腾讯云 COS + CDN（`AppConfig.videoBaseURL`），地址规则 `{videoBaseURL}/movements/{动作id}.mp4`。新增视频只需上传到 COS 并把动作 id 加入 `SkillLibrary.skillsWithVideo` 白名单即可生效（白名单手动维护，避免被 `gen_skilldata.py` 覆盖）。
- **本地缓存**：新增 `MovementVideoCache`，首次播放边下边播、下完整段缓存到 `Caches/MovementVideos/`，再次进入秒开且离线可用；缓存设 800MB LRU 上限，超限按最近访问淘汰最旧文件。下载失败 / 无网时优雅回退到占位符。
- 首批接入 `row`（划船）一个动作做联调，后续动作陆续补齐。

## 1.9.6版本 - 2026-06-19

**首页新增「近期活动」板块 + 全局绿色背景光晕**

- **近期活动时间线**：记录页首页底部（「训练时刻」下方）新增「近期活动」板块，按时间倒序聚合展示三类进展——解锁动作（🎉「动作解锁 X」）、记录某次动作完成（💪「动作训练 X N reps」）、完成 WOD（🔥「完成WOD」）。扁平竖向时间线（无卡片嵌套）仅展示最近 5 条，标题与内容间距大于行间距、与「训练时刻」保持一致；右侧显示具体日期（M.d）。
- **可点击跳转**：点击动作 / 解锁条目进入对应动作详情页（`SkillDetailView`），点击 WOD 条目进入历史记录详情页（`HistoryDetailView`）。
- **事件来源**：掌握取自 `SkillStatus`（时间用最后更新时间），完成取自 `SkillTrainingEntry`，WOD 取自 `WODRecord`；三类事件均为空时该板块自动隐藏。
- **全局背景光晕**：三个主 Tab 页（技能树 / 记录 / 我的）背景由纯黑改为「纯黑 + 右上角绿色光晕」，新增可复用的 `WTScreenBackground`，呼应产品绿色调性。
- **隐藏 iCloud 同步邀请提示**：登录后未开启 iCloud 同步时不再弹出「开启 iCloud 同步」邀请框（逻辑暂时注释，未来可恢复）。

## 1.9.5版本 - 2026-06-19

**恢复进阶路线 Tab + 技能树多项优化**

- **进阶路线 Tab 回归**：技能树首页恢复「动作 / 进阶路线」双页签（原生分段控件）。进阶路线页以扁平列表展示 5 条解锁路线（跳绳 / 倒立 / 单杠双力臂 / 引体向上 / 脚触杠），每条卡片显示路线名、掌握进度条与数字。
- **路线数据重建**：之前隐藏期间路线数据已失效（旧 step id 与重构后的动作库 id 不再匹配）。本次以 `CF解锁路径` xlsx 为数据源整体替换，新增 `scripts/gen_progressionpath.py` 从 xlsx 生成 `ProgressionPathData.swift`（与动作库同样的 xlsx→Swift 流水线）。路线不再区分体操/举重分类。
- **路线详情页**：分为「路线进阶提示」与「路线解锁」两个分节（标题/正文样式与动作详情页统一）；线路图站点与连线贯通连接、行高加大、同时展示中英文名与难度。
- **顺序解锁**：路线内动作按顺序依次解锁——前一站未掌握时，后续站点灰显并加锁图标（仍可点开查看动作详情）；标记掌握后下一站自动解锁。已独立标记的越级站点按真实状态展示，不被锁住。
- **难度分级配色**：动作右侧 L1–L5 难度条改为按难度着色，与训练强度 RPE 四色统一（L1 绿 / L2 蓝 / L3 橙 / L4·L5 红），一眼区分。配色以 `DifficultyLevel.color` 为唯一来源，全局 `TierBars` 一致生效。
- **掌握仪表盘去重**：圆环只显示掌握率百分比，右侧只保留一处「已掌握 X/总数」，下方分类改为只列其余三种状态（进行中 / 想学 / 未标记），消除重复信息。
- **二级分类页筛选优化**：状态与难度筛选框等高对齐；新增原生搜索（按动作中/英文名检索）；列表总数「共 X 个」从顶部移到列表右下角脚注。
- **动作库数据更新**：去除动作简介 / 步骤 / 要点结尾的中文句号，并修正吊环划船、徒手深蹲、折返跑三条简介。
- **动作详情页分节重排**：顺序调整为 名称难度 → 视频 → 我的状态/数据 → 最近记录 → 前置动作练习 → 简介 → 步骤 → 要点与注意 → 降阶替代。

## 1.9.4版本 - 2026-06-18

**动作库增补英文名 + 技能树视觉减负**

- **动作库英文名称**：123 个动作（体操 56 / 举重 67）补充英文名称列，二级分类页（体操 / 举重）列表行现同时展示中文（主文案）与英文（次文案，分两行）。数据仍由 `scripts/gen_skilldata.py` 从最新 xlsx 生成。
- **筛选按钮改描边样式**：动作浏览页的状态 / 难度筛选按钮由绿色实心改为「高亮描边、中间透明」（对齐记录 WOD 页样式），降低整页绿色锚点。
- **动作详情页减负**：「我的状态」三个按钮与「记录一次」按钮改为描边高亮样式；「我的状态 / 我的数据 / 动作简介 / 动作步骤」等分节标题改用与记录详情页（如「WOD 内容」）一致的小标题样式（caption 字号 + 次要灰、无绿色竖线），属于次级页面内部分节标题。
- **技能树首页标题精简**：「分类 / 进行中」页面级标题去掉左侧绿色竖线，「进行中」不再在右侧展示数字，整体更接近原生页面级标题。

## 1.9.3版本 - 2026-06-18

**修复：正式版 iCloud 同步不生效 + 开启同步后技能树崩溃**

- **正式版多设备同步打通**：此前 App Store 正式版的 iCloud 数据始终拉不到第二台设备——根因是 CloudKit 的 Production 环境从未部署 schema（正式版不允许客户端自动建表），数据其实一直只存在本地。现已在 CloudKit Console 将 schema 部署到 Production，同一 iCloud 账号的多台设备可正常互相同步训练记录、技能状态与昵称头像。
- **修复开启同步后技能树闪退**：多设备合并时同一动作可能出现重复状态行（如默认已掌握的「熊爬」），在后台去重完成前进入技能树会崩溃。现已让相关页面容忍重复行（保留最新一条），不再闪退。

## 1.9.2版本 - 2026-06-18

**修复：分享后训练记录未保存**

- 记录 WOD 流程最后一页改为进入页面即落库（与「保存成功」文案一致）：此前只有点「完成」才会保存，从「分享到社交媒体」路径离开时记录从未写入训练时刻。现在无论点哪个按钮或直接返回，记录都会保存（带幂等保护，避免重复保存）。

## 1.9.1版本 - 2026-06-16

**账号删除入口（App Store 审核 5.1.1(v) 整改）**

- **App 内删除账号**：「我的」页登录后新增「删除账号」原生确认页，说明删除流程（服务端移除数据 / 30 天内可邮件撤回 / 逾期正式注销），展示并可复制账号标识，点「确定删除」自动复制标识并用系统浏览器打开删除申请表单——满足审核「无需发邮件即可在 App 内发起删除」的要求。
- 保留「账号注销说明」文档入口，与上述原生发起流程并存：文档负责说明、按钮负责发起。

## 1.9.0版本 - 2026-06-16

**技能树重构 · 总览仪表盘 + 动作库数据更新（123 动作）**

- **总览仪表盘（新增主页）**：技能树落地页置顶掌握环 + 四态读数（已掌握 / 进行中 / 想学 / 未标记）+ 分类模块（体操 / 举重）+「进行中」列表。原生大标题，随滚动折叠，与记录 / 我的页一致。
- **四态视觉可分**：新增 `StatusGlyph` 作单一可信源 —— ✓ 绿（已掌握）/ ◐ 蓝半圈（进行中）/ ○ 灰描边（想学）/ ⊘ 虚线暗（未标记），修正旧版「未标记 / 想学」混画问题。
- **动作浏览页（独立页面）**：点分类卡进入，原生返回 + 状态 / 难度筛选 + 按难度排序的扁平列表（不再按二级分类分组）。
- **动作详情页**：状态切换、我的数据三宫格 + 整宽「记录一次」按钮、「前置动作练习」（仅展示前置动作）、最近记录、动作简介 / 步骤 / 要点 / 降阶替代；记录弹窗加高避免按钮被截断。
- **动作库数据更新**：依据最新整理的 123 个动作（体操 56 / 举重 67）重建数据模型，新增「动作模式 / 计量方式 / 简介 / 步骤 / 要点 / 降阶替代 / 前置依据」等字段；数据由 `scripts/gen_skilldata.py` 从表格生成。
- **新用户默认进度**：首启自动把仰卧起坐 / 熊爬 / 划船 / 单摇跳绳 / 农夫行走标记为已掌握，俯卧撑 / 徒手深蹲 / 哑铃硬拉标记为进行中，避免主页空白。
- 视觉统一为深色 + 单色荧光绿（进行中蓝为唯一功能色），去除冗余装饰与英文标签。
- 设计系统补强：`WTFont.mono`、`Color.wtInProgress`、`SkillSectionHeader`、`TierBars`、`MasteryRing` 等（[Theme.swift](WODTrack/Views/Common/Theme.swift) / `SkillTreeComponents.swift`）。
- 进阶路径（地铁线路图）暂随本次数据更新隐藏，待主线数据就绪后恢复。

## 1.8.9版本 - 2026-06-12

**上架前合规入口与首启协议确认**

- 新增首次打开 App 的用户协议确认页：展示《用户协议》《隐私政策》入口，用户点击「同意并继续」后写入本地同意状态；未同意时停留在确认页并提示无法继续使用。
- 接入线上合规文档：用户协议、隐私政策、技术支持与常见问题、账号注销说明、反馈表单统一使用 App 内 Safari 页面打开。
- 「我的」页补齐合规入口：技术支持与常见问题、反馈、账号注销说明、用户协议、隐私政策均可直接访问，移除反馈占位提示。
- 登录页协议链接改为打开同一套线上文档，避免 App 内静态文本与发布文档不一致。
- App `MARKETING_VERSION` 调整为 `1.0.0`；保留 `CURRENT_PROJECT_VERSION` 不变。
- 单元测试补充合规链接配置与首启同意持久化覆盖，并同步修正录入流程重构后的旧测试 API。

## 彩蛋 - 训练时刻缩略图掉落堆叠

- 在「记录」主页面，**3 秒内连点 tabbar 的「记录」标签 5 次**触发隐藏彩蛋：所有训练时刻缩略图从屏幕顶部错峰掉落、旋转散落堆叠成一摞，约 2–3 秒后自动淡出。
- 触发瞬间伴随一次成功震动反馈；纯娱乐，不影响任何数据与既有流程。记录为空时不触发。
- 新增 `PhotoDropEasterEggView`（复用首页 `HistoryThumbnail` 取图，无照片走渐变占位）；`ContentView` 用代理 `Binding` 检测同 tab 连点，至多渲染前 80 张缩略图。

## 1.8.6版本 - 2026-06-12

**历史记录全部页 → 时间线列表（第二阶段）**

- 全部页（点首页「训练时刻」进入）从文字主导的 plain List 改为**竖向时间线列表**：左侧连续轨道（竖线 + 圆点），**按年份大标题分组**。
- 每条记录展示：**日期 + 星期、缩略图、成绩、RX/SC 徽标、难度 4 格条**（难度条画法与卡片分享页一致）。`wodDate` 仅有日期，故日期下用星期而非时间。
- 新增 `HistoryTimelineRow` / `TimelineRailRow` / `DifficultyDots` / `ScoreScalingBadge`；移除旧的 `HistoryRecordCard` 与 `DifficultyStars`。
- 全部页 `@Query` 排序由 `createdAt` 改为 **`wodDate`（训练时间）**，与首页一致；保留侧滑删除、右上角月份筛选与数量统计；首页热力图点某天跳转（`scrollToDate`）仍生效。
- 列表行右侧信息精简为「难度 emoji + 成绩 + WOD 内容两行」；难度由绿色格条改为 emoji、RX/SC 不再单列展示，绿色仅保留时间轴圆点。

**记录详情页视觉改造**

- 重排版式：**日期大标题置顶** → 卡片预览图（竖图限高 420、居中，避免占满屏）→ **2×2 数据概览**（成绩 / 类型 / 标准 RX·SC / 强度 emoji）→ WOD 内容卡 / 备注卡 → 打卡时间脚注 → 底部成组的「保存到相册 + 删除」。
- 难度统一为 emoji + 等级；配色中性、绿色克制，与列表/首页一致。

## 1.8.5版本 - 2026-06-12

**首页历史记录 → 图片流「训练时刻」（第一阶段）**

- 首页「历史记录」从文字主导的竖排卡片改为**以图片为主的横滑图片流**，并改名为「**训练时刻**」：一屏约露 3.5 张、**上下两行**横向滚动，更突出升级后的卡片图 / 打卡照。
- 新增 `HistoryPhotoCard`：统一竖图比例（3:4），复用既有取图逻辑（卡片图 → 首张打卡照 → 渐变兜底）；底部深色渐变信息条叠加**日期 + 成绩数值 + 难度 emoji**（沿用录入流程的 😌🙂😤🔥）。
- **灵动排布**：两行各自独立排布、共享同一横向滚动（不强制对齐），每卡按全局索引做确定性的轻微倾斜 + 上下/左右错位（长度 7 的参数表，与每行 6 张错开避免镜像呆板，滚动不抖动）。
- **视觉收敛**：卡片边框 / 发光改中性白（绿色让位给「记录 WOD」按钮），照片轻微去饱和（0.8），避免图片流与主操作按钮抢眼。
- 首页排序由 `createdAt`（录入时间）改为 **`wodDate`（训练时间）** 降序。
- 历史记录全部页（`HistoryListView`）暂未改动，留待第二阶段。

## 1.8.4版本 - 2026-06-12

**生成训练卡片页：配色主题 + 边框留白模板（含「@迹录 WOD」水印）**

- 「生成训练卡片」页底部新增**「配色」tab**：6 套配色主题（浅色 / 深色 / 荧光绿 / 胶片 / 海蓝 / 高对比），一键换肤。配色主题**作用于文字色 + 卡底/留白色 + 强调色**（成绩类型标签、难度条等）；面板型模板的信息卡底色、边框版的色框底色都随主题切换。
- **「文字」tab 保留微调**：字体 / 字号 / 透明度仍可手动调（配色主题为主、手动为辅）；切模板会带出该模板协调的默认主题，用户可再覆盖。
- 新增 **「边框留白」模板（免费，排在第一位）**：照片缩成圆角块、四周留出有色边框（边框底色跟配色主题联动，默认荧光绿），**数据仍压在照片上**（带暗角，不挤进留白），不必担心文字溢出。边框版日期与 WOD 内容同字体同白色，避免灰字在浅色留白上看不清。
- **随机初始模板**：每次进入应用，「生成训练卡片」从 边框留白 / 训练日志 / 数据仪表盘 中随机选一个作为初始模板。
- **「@迹录 WOD」水印**：卡片右下角渲染品牌水印，**免费用户保留、Pro 用户去除**；文字缩小并贴紧右下角，去掉绿色标记。预览画布与导出图一致。
- `WODRecord` 增量新增顶层字段 `colorThemeId`（默认「深色」，与 `enabledModules` 同为带默认值的轻量迁移字段，不放进 `TextLayout` 复合属性内以避免 SwiftData 迁移报错）；老记录缺字段自动回落深色，历史卡不受影响。
- 精简模板：移除「拍立得」（与边框留白效果重复）与 Pro「杂志封面」。

## 1.8.3版本 - 2026-06-11

**生成训练卡片页「模块化」—— 已采集字段渲染进卡片、可开关**

- 「生成训练卡片」页底部新增**「模块」tab**：成绩（含 RX/SC 标记）、难度、日期、WOD 内容、备注可逐项开关，控制其是否出现在卡片上；卡面按固定顺序渲染（日期 → 成绩 → 难度 → WOD 内容 → 备注），开关只显示 / 隐藏、不挪动其它模块位置。
- 卡片渲染补上 v1.8.2 已采集但此前未用上的字段：成绩用大号等宽数字 + 类型标签 + RX/SC 徽标，难度按 4 级渲染，备注以斜体小字呈现；无数据的模块即便开启也不渲染、不留空行。
- **先模板后模块**：各模板声明自己有版位的模块，模板不支持的模块在「模块」tab 中灰掉锁住、不可开启，画布也不渲染（如「胶片复古」仅支持日期 / 成绩 / WOD 内容）。
- 实时预览修复：编辑页画布此前漏传成绩 / 备注等字段，导致对应模块恒空，现已补齐。
- `WODRecord` 增量新增非可选字段 `enabledModules`（默认日期 / 成绩 / 难度 / WOD 内容；CloudKit 安全的轻量迁移，上架前随整体 schema 一起部署 Production）。
- 卡面文字自适应缩放把模块块高度一并计入（成绩 / 难度 / 备注折算等效行数），并在开关模块时即时重算，修复加模块后文字超出图片（尤其「胶片复古」）的问题。
- **左右位置改为全模板通用**：所有模板的文字 / 模块都可在「位置」面板切左 / 右（此前仅夜训卡片等少数支持），各模板保留各自默认朝向。
- **默认打卡照**：内置改为横屏 + 竖屏两张，未选照片进入「生成卡片」时随机轮换其一。
- **键盘**：全 App 输入页支持点击空白处收起键盘；录入表单支持下拉收起。
- 配色主题（6 套）、边框画布版模板、「迹录」水印（免费保留 / Pro 去除）留到后续版本。

## 1.8.2版本（续2） - 2026-06-11

**优化「选择训练打卡照 → 生成训练卡片」两步交互**

- 选择打卡照页：文案改为「选择一张训练打卡照，将 WOD 内容合成为分享卡片」；「导入训练照片」按钮配色由系统蓝改为品牌色，与全局统一。
- 照片不再强制选择：新增内置默认照片（`DefaultCheckinPhoto`），未选照片时点击「下一步」自动用默认照片合成卡片；底部加一行说明，去掉单独的「使用默认图片」按钮避免与「下一步」二选一的歧义。
- 去掉「上一步」按钮（统一由左上角返回），「下一步」文案改为「下一步：生成训练卡片」；「已选照片」缩略图去掉多余的序号「1」（仅允许一张）。
- 最终预览页标题由「最终预览」改为「生成训练卡片」，「保存」按钮改为「生成」；按钮高度由 44 调为 36，并将顶部 header 的 padding 与前两步对齐，消除步骤切换时的高度跳变。

## 1.8.2版本（续） - 2026-06-11

**记录流程合并：步骤一并入步骤二**

- 取消独立的「记录今日 WOD」入口页（拍照 / 相册 / 手动三选一），从首页「记录 WOD」直接进入内容页；页面标题由「今日 WOD 内容」改为「记录 WOD」。
- 内容页 WOD 输入框下方新增「自动识别 WOD 内容」按钮：点击弹出「立即拍摄 / 从相册选取」，开始 loading，识别完成后把内容回填到 WOD 输入框；超时 / 报错沿用原有通知与重试逻辑。
- 自动识别仅回填 WOD 内容，保留已填写的日期 / 成绩 / 强度 / 备注；取消识别（超时返回）回到内容表单并保留已输入内容。
- 内容页返回键改为关闭整个录入流程（已是第一步）。清理：移除 `WhiteboardStep` 入口与 `startFlow` / `startManualEntry`。

## 1.8.2版本 - 2026-06-11

**优化「填写 WOD」录入 —— 结构化字段补齐**

- 「填写 WOD」页面字段重排并扩充，从上到下：时间（可编辑训练日期）→ WOD 内容（输入框加高）→ WOD 类型 → 成绩 → 强度 → 完成时间 → 备注。
- 新增**可编辑训练日期**：默认今天，支持「今天 / 昨天 / 前天」快捷选择与具体日期选择（禁选未来），用于补录前几天的训练，写入 `wodDate`。
- 新增 **WOD 类型 + 成绩**：类型支持 For Time / AMRAP / EMOM / Max Load，成绩输入随类型变样（时间 mm:ss / 完成次数 / 轮次±reps / 重量 kg），并带 **RX / SC / 未标记** 三态标记（默认未标记，可整体留空）。
- **强度**由 1–5 星改为 4 级抽象（轻松 / 适中 / 困难 / 挑战，对应 RPE 每 25% 一档）；历史里的旧 1–5 数据按 4 级映射展示（5→挑战）。
- 新增可选**备注**自由文本。历史详情同步展示成绩（含 RX/SC）、4 级强度与备注。
- `WODRecord` 增量新增可选字段 `scoreType / scoreValue / scoreScaling / note`（CloudKit 安全的轻量迁移，上架前随整体 schema 一起部署 Production）；`difficultyRating` 复用为 1–4。
- 清理：OCR 接口的 `wod_type` 始终返回「其他」，已从 `OCRResult` 移除并停止解析（`WODType` 枚举保留，复用为「WOD 类型」选择器）。

## 1.8.1版本（续3） - 2026-06-11

**图片识别改走自建 Serverless 代理**

- 白板 OCR 不再由 App 直连火山方舟（豆包），改为调用自建 Serverless 代理（火山 API Gateway，`POST /ocr`）；火山 API Key、system prompt、模型参数全部收归服务端，客户端不再持有任何密钥。
- 装配从 `DoubaoLLMService.shared` 切到既有的 `OCRService.shared`：发送 `{image_base64}`、解析代理返回的结构化 JSON `{wod_type, wod_content, confidence}`（非流式）；UI、ViewModel、`OCRResult` 与 `OCRServicing` 协议均不变。
- 鉴权预留：`OCRService` 改为可选 `X-App-Secret` 头（仅当 `AppConfig.appToken` 非空时发送），代理当前未启用校验，后续开启 `APP_SHARED_SECRET` 后填同值即可。
- 删除 `DoubaoLLMService.swift` 及客户端硬编码的火山 Key。⚠️ 已泄露的旧 Key 需在火山控制台单独吊销轮换（历史包仍含旧 Key）。

## 1.8.1版本（续2） - 2026-06-10

**头像与昵称随 iCloud 同步**

- 新增 `SyncedProfile` @Model（昵称 + 头像选择 + 自定义头像图 externalStorage），随现有 SwiftData+CloudKit 镜像上传/拉取，受同一个「iCloud 同步」开关与登录态门控。
- 本地权威不变（UserDefaults + avatar.jpg），模型仅作云端载体：资料编辑时写穿入库并记录 `wt.profileUpdatedAt`；启动、回前台、容器重建时按 last-writer-wins 调和（云端较新→应用并落地头像文件；本地较新→写回云端行）。
- 自定义头像跨设备落地：字节未到且本地无文件时暂降级默认头像，下次调和自动补上。
- 多设备各建过资料行时由 SyncDedupService 保留 updatedAt 最新的一条（singleton 去重）。
- 首次升级自动把现有本地昵称/头像迁移入库；schema 为增量变更，CloudKit Development 环境自动出现 `CD_SyncedProfile`，上架前随整体 schema 一起部署 Production。

## 1.8.1版本（续） - 2026-06-10

**iCloud 同步（SwiftData + CloudKit 私有库镜像）**

- 「我的 → iCloud 同步」新增详情页：开关 + 系统 iCloud 账号状态 + 本地数据概览（记录条目 / 占用空间 / 导出占位）。开启后本地全部数据（打卡记录+合成卡片图、技能状态、技能训练记录）自动上传用户私人 iCloud，多设备并集合并；数据不经过开发者服务器。
- 三道门：App 登录（未登录点行→登录页，登录成功后直接进同步详情页）；系统 iCloud 账号（不可用时开关禁用并引导去系统设置）；网络（CloudKit 自动排队重试）。
- 模型 CloudKit 化：移除全部 `@Attribute(.unique)`（改由 SyncDedupService 应用层去重：SkillStatus 按 skillId 留最新、WODRecord 按 id、SkillTrainingEntry 按新增 entryId）；非可选属性补声明处默认值；WODRecord 新增 `cardImageData`（externalStorage）。同店名 WODTrackStore_v3 轻量迁移，存量数据无损。
- 卡片图同步：保存时文件+Data 双写；跨设备文件缺失时从云端 Data 重建并落地（`ImagePathResolver.loadCardImage`）；开启同步时后台回填旧记录。打卡照片原图不进云。
- 运行时开关：重建 ModelContainer 切换 `.private`/`.none`（无需重启），ContentView 按容器代数整树重建；同步默认关闭，开关仅在 accountStatus 可用时可开（规避无 entitlement 异步崩溃陷阱）。
- 退出登录 = 同步暂停（意图保留，重新登录自动恢复）；关闭开关需二次确认，明确不删本机与云端数据。
- 登录后未开同步时「我的」页显示虚线邀请卡（立即开启 / 以后再说-本会话隐藏）。
- 工程：entitlements 新增 CloudKit + 容器 `iCloud.com.chenxi.WODTrack` + aps-environment；Info.plist 新增 remote-notification 后台模式。真机同步需在 Xcode 切付费团队并确认 iCloud/Push capability；上架前在 CloudKit Console 将 schema 部署到 Production。

## 1.8.1版本 - 2026-06-10

**登录流程 + 「我的」页重建**

- 新增独立全屏登录页：深色背景 + 顶部绿色辉光，Logo「迹」+「迹录 WOD」+ slogan；仅支持 Sign in with Apple（系统原生授权，scope 仅姓名不取邮箱）；协议勾选为前置条件（未勾选点登录会拦截并提示）；支持「暂不登录」与左上角关闭退出。
- 「我的」页整体重建：未登录顶卡为登录入口（「登录迹录」+ 副标题 + chevron）；已登录顶卡显示头像（相机角标）+ 昵称大标题，右上铅笔进入编辑。
- 头像：内置 2 个默认头像（杠铃 / 奖杯，2 选 1）+ 支持相册上传自定义头像（方形裁剪压缩后存本地）；昵称默认取 Apple 姓名（仅首次授权返回），可在编辑面板修改；昵称、头像均存本地，仅登录后可编辑。
- 登录态持久化：Apple 用户标识存 Keychain（卸载重装可恢复）；启动时校验凭证，仅被撤销（revoked）时自动登出；运行中收到系统撤销通知同样登出；底部新增红色「退出登录」行（带确认弹窗，退出不清训练数据）。
- 记录保存后的登录引导改为弹出新全屏登录页（替换原微信登录占位 sheet，微信相关代码与 `wtWechatGreen` 移除）。
- 「我的」页分组：数据（iCloud 同步——未登录显示「未登录」点击跳登录、已登录显示「未开启」占位，本期不做 iCloud 对接；本地数据备份——占位「即将推出」）；其他（关于我们、反馈——占位「即将推出」、隐私协议）。偏好设置与 PRO 会员整组本期隐藏。
- 新增「关于我们」页（Logo + 版本号）与本地静态《用户协议》《隐私政策》文本页。
- 新增全局 toast 组件（`wtToast`），登录成功等轻提示统一样式。
- 工程：新增 Sign in with Apple entitlement（真机如签名失败需在 Xcode → Signing & Capabilities 确认 capability 已注册）。

## 1.7.8版本 - 2026-06-10

**历史记录：删除功能 + 详情页重构**

- 历史记录列表支持左滑删除：滑出红色垃圾桶按钮，确认后删除（iOS 原生交互）；首页历史卡片不受影响。
- 记录详情页底部新增「删除记录」按钮；两处删除均弹居中确认弹窗，删除时同步清理本机的卡片图片与照片文件。
- 详情页新增「保存图片到相册」：随时可重新下载合成卡片，成功后按钮短暂显示「已保存」。
- 详情页信息区重构为统一字段行：难度、训练用时（分钟）、打卡时间（完整年月日 + 时间）；移除训练照片原图展示区。
- 新记录不再把训练照片原图写入磁盘，仅保存合成卡片，节省存储空间；旧记录的原图文件在删除该记录时一并清理。
- 数据模型移除无录入入口的 `wodType`（训练类型）与 `completionStatus`（完成状态）字段及相关枚举，避免后续需求误引用；`WODType` 枚举保留供 OCR 解析使用。
- 新增 `RecordDeletionService`（删除记录 + 清理图片）与 `PhotoLibraryService`（相册保存），记录流程的相册保存改为复用后者。

## 1.7.7版本 - 2026-06-09

**记录页视觉重心收敛**

- 减少首页绿色锚点：热力图顶部统计的图标弱化为灰色，与标签组成「图标+标签」一行，数字保留主题绿。
- 取消统计分栏的黑色块背景，改用短竖线分隔三栏。
- 新增第三个统计「训练次数」（全部历史记录条数）；三项标签精简为「连续打卡 / 累计打卡 / 训练次数」避免折行。
- 历史记录卡片改为「左图 / 右侧信息」横向布局：左侧照片，右侧顶部为日期 + 难度星级，下方训练内容；日期颜色由绿色改为白色。
- 整页视觉重心收敛到热力图与「记录 WOD」按钮。

## 1.7.6版本 - 2026-06-09

**品牌色焕新**

- 全局主题色从金黄色 `#F5C518` 调整为荧光绿 `#5BE88F`，灵感来自「记录」首字母 J 的发光 Logo，整体视觉与品牌统一。
- 同步更新 `AccentColor`，系统强调色（控件 tint 等）一并变绿。
- 主题色为单点定义（`Color.wtPrimary`），底部 Tab、主按钮、记录流程评分星标、技能树选中态与已掌握标记、历史页高亮等全部自动焕新。
- 「成功」「微信绿」等语义色及彩带特效配色保持不变。

## 1.7.5版本 - 2026-06-09

**技能树 · 动作库重构**

- 动作库从「体操 / 举重 / 有氧」三类重构为 CrossFit 标准的「体操 / 举重」双大类，共 84 个标准动作（体操 49 + 举重 35）。
- 每个大类下再细分子类别（体操的拉类、推类、倒立、核心、技巧，举重的深蹲、推举、硬拉、上挺、抓举等），难度体系扩展为 L1~L5 五级，并为动作标注前置动作与量化解锁条件。
- 技能树页改为「动作 / 进阶路径」双 Tab（iOS 原生 segmented Picker）：两个维度分离，切换器随内容一起向上滚动、不再固定置顶，导航栏保留「技能树」标题与原生毛玻璃背景。
- 难度标签（L1-L5）移到动作名左侧，右侧留出个人成绩（RPT）数据空间。
- 子类别与难度整合为第二层筛选，选中后扁平展示对应动作，不必展开大卡片或长滚；状态计数随筛选实时联动。
- 新增「进阶路径」模块：11 条主线路径（体操 6 + 举重 5）按大类分组，每条主线展示掌握进度条，点击查看从入门到精英的完整升级链。
- 修复三处「整行（含空白区域）均可点击跳转」：技能树主列表、详情页前置动作、进阶路径动作，不再只响应文字和箭头部分。

**其他**

- 应用显示名正式定为「迹录」，呼应 slogan「每一次进步，都有迹可循」。

## 1.7.4版本 - 2026-06-08

**记录 WOD 流程体验优化**

- 修复关闭记录流程后底部 Tab Bar 先消失再出现的闪烁问题（改用 `fullScreenCover` 呈现）。
- 统一各步骤页头样式：关闭/返回按钮与标题均左对齐，保存按钮固定在右侧。
- 「拍摄」「手动输入」两张选项卡整体可点击，卡片高度缩小、右下角加半透明装饰图标。
- OCR Loading 页面新增三段文案轮播（正在抓取→处理→转化为文字），每 2.2 秒切换，缓解等待焦虑。
- OCR 识别超时（10 秒）后底部滑入「返回上一步」和「重试」按钮，同时真实取消底层网络请求，不再需要强杀 App。
- 识别失败不再跳独立页，与识别成功共用「今日 WOD 内容」页，顶部红色 banner 展示错误原因，底部多一个「重试」按钮。
- OCR 失败错误文案全部中文化，不再透出系统英文 `localizedDescription`。
- 每次新开识别流程时自动清空上次 OCR 残留内容，避免旧数据出现在文本框。
- 网络错误自动重试最多 3 次（每次间隔 3 秒），解决首次安装尚未授权网络时直接失败的问题。
- 「今日 WOD 内容」页新增「完成时间」字段，可选 45 分钟、60 分钟或自定义分钟数；`WODRecord` 同步新增 `completionMinutes` 字段持久化。
- 难度星星整体缩小约 25%（font size 34→26，行高 52→40）。
- 「导入训练照片」整个卡片区域均可点击，不再只响应图标和文字部分。

## 1.7.3版本 - 2026-06-08

**历史记录卡片重设计**
- 日期格式改为 `yyyy.MM.dd`，固定显示不受系统语言影响，颜色改为主题黄色作为视觉锚点。
- 右上角改为难度星级（仅显示实心星，白色柔光效果），移除"已完成"badge。
- 缩略图缩小至 72×72，WOD 内容限 2 行，去掉底部难度/完成状态标签。
- 卡片加入细边框（白色 7% 透明度）+ 双层阴影（黄色柔光 + 深色投影），提升质感。
- 首页预览卡片与历史记录页统一为同一个 `HistoryRecordCard` 组件，后续只需改一处。

**训练概览热力图**
- 热力图改为横向可滑动，右边界固定为当前周，左侧最远可滑至 2026 年 1 月 1 日。
- 打开时自动滚动到最右端（当前周）。
- 格子颜色按当日记录数渐变：0 条灰色、1 条 55% 黄、2 条 78% 黄、3 条及以上完整黄色。
- 空格子不可点击，有记录的格子点击跳转对应日期。
- 月份标签修复截断为"..."的问题，现在正常显示。
- 移除"训练概览"标题，移除"近 12 周"副标题。
- 统计块加入图标（连续打卡 🔥 / 累计打卡 📅），布局更直观。

**历史记录页**
- 记录按月份分组，支持右上角下拉 Menu 按月筛选，选中状态实时更新标题旁的条数。
- 导航标题改为"历史记录 N"，N 随筛选变化。
- 移除 section header，列表更简洁。

**记录页（首页）**
- "记录今天的 WOD" 按钮移至右下角，改为悬浮胶囊形 FAB（黄底黑字，黄色光晕）。
- "历史记录"整行可点击跳转，右侧改为灰色箭头，移除黄色"查看更多"。
- 首页卡片间距与历史记录页统一为 `WTSpacing.md`。
- 导航标题改为"每一次进步，都有迹可循"。

## 1.7.2版本 - 2026-06-08

- 技能树重构为可交互的动作掌握度追踪器，支持「已掌握 / 进行中 / 想学」三态手动标记，状态通过 SwiftData 持久化。
- 技能树顶部新增状态筛选栏（带对应图标），可按掌握状态过滤动作列表，并实时显示各分类数量。
- 分类卡片新增三色进度条（黄=已掌握、蓝=进行中、灰=未标记），直观呈现整体掌握进度。
- 动作展示从三列网格改为列表形式，每行显示状态图标、中英文名称和个人纪录数值。
- 新增动作详情页：状态切换、「我的数据」摘要（个人纪录 + 上次练习日期）、训练记录历史列表（支持删除）、动作要领占位区、视频占位区（Coming Soon）。
- 新增「记录一次」填写抽屉：支持录入成绩（数值 + 单位 kg/lb/reps/seconds）、日期（内联日历，选后自动收起）、备注；打开时自动聚焦输入框弹出键盘。
- 个人纪录自动取所有训练条目的最高值，无需手动维护。
- 分类图标恢复为原版 SF Symbol 动作图标，各分类保留独立颜色（体操蓝/举重橙/有氧绿）。

## 1.7.1版本 - 2026-06-08

- 修复历史记录照片路径兼容性问题：将图片存储格式从绝对路径改为文件名，解决 Xcode 重装后照片丢失的问题。
- 新增 `ImagePathResolver`，加载时自动兼容旧格式（绝对路径）和新格式（文件名），现有用户升级后历史照片不受影响。

## 1.7版本 - 2026-06-08

- 新增四个免费模板：极简白底（浅色信息卡）、大字报（超大标题字体）、数据仪表盘（monospace 数据卡片风格）、胶片复古（胶片边框 + 琥珀色日期戳）。
- 移除「玻璃海报」模板，与「夜训卡片」风格重叠度较高。
- 将默认模板改为「训练日志」。
- 所有模板统一去掉「已完成」和「难度评分」展示，统一增加日期显示。
- 夜训卡片、极简白底、数据仪表盘：面板宽度随文字内容自动收缩，不再固定撑满。
- 夜训卡片、极简白底、数据仪表盘：新增左/右位置控制，面板可靠左或靠右锚定，两侧边距对称一致，面板内文字方向随位置同步切换。
- 修复大字报模板：副标题行字号现在跟随字号滑块完整缩放，不再有 12pt 下限；字体预设（标准/圆角/衬线/等宽）对标题和副标题均生效。
- 数据仪表盘移除 WOD 类型标签（「其他」等），内容更简洁。

## 1.6版本 - 2026-06-07

- 移除卡片水印：去掉原先自动叠加的 WOD Track 水印，保持成图画面简洁。
- 修复模板面板对齐：夜训卡片、玻璃海报、训练日志、杂志封面的名称标签改为居中对齐，与上方图标保持一致。
- 移除"图片" Tab：图片 Tab 始终为原图比例、无实际调整功能，已从底部编辑栏移除，减少干扰。
- 位置面板文字外移：顶部、居中、底部按钮的文字标签改为显示在按钮下方，不再占据可点击区域内部。
- 透明度滑条归位：文字透明度调整滑条从"位置"面板迁移到"文字"面板，与字体、字号控件统一管理。
- 保存成功页优化：顶部标题改为打勾图标 + "保存成功" / "已保存到相册"双行布局，进入页面时触发彩色礼花撒花动画。

## 1.5.1版本 - 2026-06-07

- 修复全部按钮点击区域：将所有按钮的可点击热区扩展为整个按钮框体，解决边缘区域点击失效问题。

## 1.5版本 - 2026-06-07

- 新增保存成功页：保存卡片后展示成图预览与操作入口，替换原来无反馈的直接关闭逻辑。
- 新增系统分享入口：保存成功页提供"分享到社交媒体"按钮，调起系统 Share Sheet，覆盖微信、朋友圈、小红书、复制图片等渠道。
- 新增技能树静态列表：实现体操、举重、有氧三大类动作展示，每类按 T0/T1/T2 难度分组，可折叠浏览，替换原来的"即将上线"占位页。
- 修复热力图月份标签溢出换行问题，优化标签字号与自适应缩放。
- 热力图格子支持点击跳转：点击任意日期格子导航到历史记录页，并自动滚动定位到对应日期附近。

## 1.4版本 - 2026-06-07

- 保存训练卡片时自动将生成的卡片图片同步写入系统相册，方便直接分享。
- 使用 `PHPhotoLibrary` 仅添加权限（`addOnly`），首次触发时系统弹出授权弹窗，后续保存自动同步。

## 1.3版本 - 2026-06-07

- 接入 Doubao 多模态大模型识别服务，将白板图片识别从 mock/占位流程切换为真实 LLM Vision 请求。
- 新增 `DoubaoLLMService`，支持图片压缩、base64 data URL 上传、流式响应解析与识别结果回填。
- 将白板导入入口接入系统相机拍摄，保留相册选取，并在相机不可用时给出提示。
- 补充相机权限说明，支持直接拍摄白板后进入识别流程。
- 优化大模型识别提示词，要求保留 WOD 分组和换行结构，过滤日期、姓名、Logo 等非训练内容。
- 配置 1.3 版本使用 Doubao 识别服务替代 mock OCR。

## 1.2版本 - 2026-06-07

- 优化最终预览页为图片编辑器式布局，顶部操作栏、画布预览区和底部参数区固定分区。
- 最终预览与保存导出改为基于用户原图尺寸，横图保存为横图，竖图保存为竖图，不再用固定画布裁切照片。
- 训练打卡照片入口调整为仅支持单图，移除双图拼接与填充/裁剪切换逻辑。
- 优化 WOD 文字字号自适应：长文本进入预览时自动给出合适初始字号，同时保留字号滑块手动调整能力。
- 将“夜训卡片”和“玻璃海报”在横图场景下收窄并靠右对齐，减少遮挡图片主体。
- 固定卡片文字 overlay 的画布尺寸，避免文字变大后撑开布局导致图片与文字错位。
- 修复历史记录详情中的成图预览比例，确保详情页按保存图片比例展示。

## 1.1版本 - 2026-06-07

- 将“记录今天的 WOD”流程从底部抽屉改为独立页面，避免手机端下滑误关闭。
- 为训练记录流程新增“返回上一步”，支持从最终预览返回训练照片，从训练照片返回 WOD 内容。
- 统一记录流程页面结构，固定顶部标题栏与底部操作栏，保持各步骤视觉和交互一致。
- 将“今日训练状态”和“今日难度”合并为“今日难度”，并将难度输入改为 1-5 星评级。
- 将多按钮底部操作栏改为左右排列，减少底部占用空间。
- 补充记录流程回退路径单元测试。

## 1.0版本 - 2026-06-07

- 初始化 `WODTrack` SwiftUI iOS App 工程结构。
- 完成技能树、记录、我的三个基础页签入口。
- 完成训练记录多步骤录入状态机与手动输入流程骨架。
- 接入 SwiftData 数据模型、历史记录列表、个人页与登录引导占位。
- 新增白板 OCR 服务协议、URLSession 实现骨架与拍照/OCR 技术方案文档。
- 新增卡片预览与 `ImageRenderer` 导出入口。
- 配置深色主题、主按钮、输入框、Loading 组件、`Info.plist`、资源目录、shared scheme 与单元测试 target。
- 最低系统版本统一为 `iOS 17.0`。

### 当前限制

- 本地环境只有 Command Line Tools，没有完整 Xcode，暂未进行 Xcode 编译验证。
- 微信登录、CloudKit、相册写入、真实图片权限流仍为占位实现。
- OCR API 需要服务端可用后再配置 `AppConfig`。
