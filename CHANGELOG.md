# 维护更新记录

## 1.7.2版本 - 2026-06-08

- 技能树重构为可交互的动作掌握度追踪器，支持「已掌握 / 进行中 / 想学」三态手动标记，状态通过 SwiftData 持久化。
- 技能树顶部新增状态筛选栏（带对应图标），可按掌握状态过滤动作列表，并实时显示各分类数量。
- 分类卡片新增三色进度条（黄=已掌握、蓝=进行中、灰=未标记），直观呈现整体掌握进度。
- 动作展示从三列网格改为列表形式，每行显示状态图标、中英文名称和个人纪录数值。
- 新增动作详情页：状态切换、「我的数据」摘要（个人纪录 + 上次练习日期）、训练记录历史列表（支持删除）、动作要领占位区、视频占位区（Coming Soon）。
- 新增「记录一次」填写抽屉：支持录入成绩（数值 + 单位 kg/lb/reps/seconds）、日期（内联日历，选后自动收起）、备注；打开时自动聚焦输入框弹出键盘。
- 个人纪录自动取所有训练条目的最高值，无需手动维护。
- 分类图标恢复为原版 SF Symbol 动作图标，各分类保留独立颜色（体操蓝/举重橙/有氧绿）。

## 1.7.1版本 - 2026-06-08

- 修复历史记录照片路径兼容性问题：将图片存储格式从绝对路径改为文件名，解决 Xcode 重装后照片丢失的问题。
- 新增 `ImagePathResolver`，加载时自动兼容旧格式（绝对路径）和新格式（文件名），现有用户升级后历史照片不受影响。

## 1.7版本 - 2026-06-08

- 新增四个免费模板：极简白底（浅色信息卡）、大字报（超大标题字体）、数据仪表盘（monospace 数据卡片风格）、胶片复古（胶片边框 + 琥珀色日期戳）。
- 移除「玻璃海报」模板，与「夜训卡片」风格重叠度较高。
- 将默认模板改为「训练日志」。
- 所有模板统一去掉「已完成」和「难度评分」展示，统一增加日期显示。
- 夜训卡片、极简白底、数据仪表盘：面板宽度随文字内容自动收缩，不再固定撑满。
- 夜训卡片、极简白底、数据仪表盘：新增左/右位置控制，面板可靠左或靠右锚定，两侧边距对称一致，面板内文字方向随位置同步切换。
- 修复大字报模板：副标题行字号现在跟随字号滑块完整缩放，不再有 12pt 下限；字体预设（标准/圆角/衬线/等宽）对标题和副标题均生效。
- 数据仪表盘移除 WOD 类型标签（「其他」等），内容更简洁。

## 1.6版本 - 2026-06-07

- 移除卡片水印：去掉原先自动叠加的 WOD Track 水印，保持成图画面简洁。
- 修复模板面板对齐：夜训卡片、玻璃海报、训练日志、杂志封面的名称标签改为居中对齐，与上方图标保持一致。
- 移除"图片" Tab：图片 Tab 始终为原图比例、无实际调整功能，已从底部编辑栏移除，减少干扰。
- 位置面板文字外移：顶部、居中、底部按钮的文字标签改为显示在按钮下方，不再占据可点击区域内部。
- 透明度滑条归位：文字透明度调整滑条从"位置"面板迁移到"文字"面板，与字体、字号控件统一管理。
- 保存成功页优化：顶部标题改为打勾图标 + "保存成功" / "已保存到相册"双行布局，进入页面时触发彩色礼花撒花动画。

## 1.5.1版本 - 2026-06-07

- 修复全部按钮点击区域：将所有按钮的可点击热区扩展为整个按钮框体，解决边缘区域点击失效问题。

## 1.5版本 - 2026-06-07

- 新增保存成功页：保存卡片后展示成图预览与操作入口，替换原来无反馈的直接关闭逻辑。
- 新增系统分享入口：保存成功页提供"分享到社交媒体"按钮，调起系统 Share Sheet，覆盖微信、朋友圈、小红书、复制图片等渠道。
- 新增技能树静态列表：实现体操、举重、有氧三大类动作展示，每类按 T0/T1/T2 难度分组，可折叠浏览，替换原来的"即将上线"占位页。
- 修复热力图月份标签溢出换行问题，优化标签字号与自适应缩放。
- 热力图格子支持点击跳转：点击任意日期格子导航到历史记录页，并自动滚动定位到对应日期附近。

## 1.4版本 - 2026-06-07

- 保存训练卡片时自动将生成的卡片图片同步写入系统相册，方便直接分享。
- 使用 `PHPhotoLibrary` 仅添加权限（`addOnly`），首次触发时系统弹出授权弹窗，后续保存自动同步。

## 1.3版本 - 2026-06-07

- 接入 Doubao 多模态大模型识别服务，将白板图片识别从 mock/占位流程切换为真实 LLM Vision 请求。
- 新增 `DoubaoLLMService`，支持图片压缩、base64 data URL 上传、流式响应解析与识别结果回填。
- 将白板导入入口接入系统相机拍摄，保留相册选取，并在相机不可用时给出提示。
- 补充相机权限说明，支持直接拍摄白板后进入识别流程。
- 优化大模型识别提示词，要求保留 WOD 分组和换行结构，过滤日期、姓名、Logo 等非训练内容。
- 配置 1.3 版本使用 Doubao 识别服务替代 mock OCR。

## 1.2版本 - 2026-06-07

- 优化最终预览页为图片编辑器式布局，顶部操作栏、画布预览区和底部参数区固定分区。
- 最终预览与保存导出改为基于用户原图尺寸，横图保存为横图，竖图保存为竖图，不再用固定画布裁切照片。
- 训练打卡照片入口调整为仅支持单图，移除双图拼接与填充/裁剪切换逻辑。
- 优化 WOD 文字字号自适应：长文本进入预览时自动给出合适初始字号，同时保留字号滑块手动调整能力。
- 将“夜训卡片”和“玻璃海报”在横图场景下收窄并靠右对齐，减少遮挡图片主体。
- 固定卡片文字 overlay 的画布尺寸，避免文字变大后撑开布局导致图片与文字错位。
- 修复历史记录详情中的成图预览比例，确保详情页按保存图片比例展示。

## 1.1版本 - 2026-06-07

- 将“记录今天的 WOD”流程从底部抽屉改为独立页面，避免手机端下滑误关闭。
- 为训练记录流程新增“返回上一步”，支持从最终预览返回训练照片，从训练照片返回 WOD 内容。
- 统一记录流程页面结构，固定顶部标题栏与底部操作栏，保持各步骤视觉和交互一致。
- 将“今日训练状态”和“今日难度”合并为“今日难度”，并将难度输入改为 1-5 星评级。
- 将多按钮底部操作栏改为左右排列，减少底部占用空间。
- 补充记录流程回退路径单元测试。

## 1.0版本 - 2026-06-07

- 初始化 `WODTrack` SwiftUI iOS App 工程结构。
- 完成技能树、记录、我的三个基础页签入口。
- 完成训练记录多步骤录入状态机与手动输入流程骨架。
- 接入 SwiftData 数据模型、历史记录列表、个人页与登录引导占位。
- 新增白板 OCR 服务协议、URLSession 实现骨架与拍照/OCR 技术方案文档。
- 新增卡片预览与 `ImageRenderer` 导出入口。
- 配置深色主题、主按钮、输入框、Loading 组件、`Info.plist`、资源目录、shared scheme 与单元测试 target。
- 最低系统版本统一为 `iOS 17.0`。

### 当前限制

- 本地环境只有 Command Line Tools，没有完整 Xcode，暂未进行 Xcode 编译验证。
- 微信登录、CloudKit、相册写入、真实图片权限流仍为占位实现。
- OCR API 需要服务端可用后再配置 `AppConfig`。
